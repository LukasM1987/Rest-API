DROP PROCEDURE IF EXISTS GetUserById;
DELIMITER $$
CREATE PROCEDURE GetUserById(id INT)
BEGIN
    IF id > 0 THEN
        SELECT FIRSTNAME, LASTNAME FROM READERS WHERE READER_ID = id;
    ELSE
        SELECT 'User does not exist' FROM READERS LIMIT 1;
    END IF;
END $$
DELIMITER ;
CALL GetUserById(0);